# Selenium-POM-Framework
Hybrid Test Automation Framework for a ToDo Web App built with Java, Selenium WebDriver, TestNG, and RestAssured. Uses Page Object Model (POM) and API-driven test setup (injecting cookies) for fast execution, integrated with Allure Reports.
